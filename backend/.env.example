# PES 환경 변수 설정 예시
# 이 파일을 복사해서 .env로 만들고 실제 값으로 변경하세요

# ===========================================
#  필수 설정 (보안 관련)
# ===========================================

# 행정안전부 재난문자 API 키 (필수)
# https://www.safetydata.go.kr 에서 발급받으세요
DISASTER_API_KEY=your_service_key_here

# JWT 시크릿 키 (필수)
# 프로덕션에서는 반드시 강력한 랜덤 문자열로 변경하세요
SECRET_KEY=your-secret-key-change-in-production

# ===========================================
#  네트워크 설정 (Docker 환경에서 변경 가능)
# ===========================================

# PostgreSQL 데이터베이스 연결 URL
DATABASE_URL=postgresql+asyncpg://pes_user:pes_password@localhost:5432/pes

# Redis 캐시 연결 URL
REDIS_URL=redis://localhost:6379

# Ollama LLM 서비스 엔드포인트
OLLAMA_ENDPOINT=http://localhost:11434

# ===========================================
#  선택적 설정
# ===========================================

# Firebase Cloud Messaging 인증 파일 경로 (FCM 사용 시)
FIREBASE_CREDENTIALS_PATH=./credentials.json

# Google Maps API 키 (향후 사용, 선택)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Sentry 모니터링 DSN (선택)
SENTRY_DSN=your_sentry_dsn

# ===========================================
#  개발 설정 (선택적)
# ===========================================

# 디버그 모드 (개발: True, 프로덕션: False)
DEBUG=True

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 재난문자 폴링 주기 (초)
DISASTER_POLL_INTERVAL_SECONDS=10

# Mock Data Configuration (재난 API 점검 시)
USE_MOCK_DATA=true
MOCK_DATA_PATH=app/data/sample_disasters.csv
